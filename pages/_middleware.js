import { NextResponse } from 'next/server'

export async function middleware(req) {
  // if (req.nextUrl.pathname === '/' && !req.cookies.token) {
  //   return NextResponse.redirect('http://localhost:3000/login')
  // }
}
